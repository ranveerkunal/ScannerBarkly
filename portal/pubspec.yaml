name: portal
description: ScannerBarkly
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  color_models: ^1.2.1
  cupertino_icons: ^1.0.2
  flutter_markdown: ^0.6.2
  google_fonts: ^2.0.0
  http: ^0.13.1
  path: ^1.8.0
  provider: ^5.0.0
  url_launcher: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

assets:
  - data/
